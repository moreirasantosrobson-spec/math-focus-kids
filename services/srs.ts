// services/srs.ts

type d'exportation ReviewItem = {
id : chaîne de caractères ;
dueAt : numéro ;
};

/**
* Renvoie pour l'instant une liste vide (ébauche).
Ensuite, nous implémenterons la logique SRS proprement dite.
Tentatives de connexion de l'utilisateur.
*/
export function getDueForReviewItems(): ReviewItem[] {
retour [];
}
